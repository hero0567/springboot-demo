server:
  port: 8088

spring:
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/hero0567/springboot-demo
          search-paths: config
          #username: username  # 以及相应的账户名
          #password: password  # 和密码
          #basedir: E:\Java_IDEA\config\basedir  # 可以使用这个配置项来指定本地git仓库的路径